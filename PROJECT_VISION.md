# NeuroGrid 项目构想与开发说明

> **项目状态：** 概念设计 / M0 项目初始化  
> **项目类型：** AI 驱动的在线多人战术 Roguelike  
> **核心技术：** Godot、FastAPI、WebSocket、PostgreSQL、PyTorch、React、Docker  
> **开发方式：** 个人主导，借助 AI 辅助设计、编码、测试和文档整理  
> **首要原则：** 先完成可玩的完整闭环，再逐步加入联机、数据系统和智能 AI

---

## 1. 项目是什么

NeuroGrid 是一个基于网格地图的轻量战术 Roguelike 游戏。玩家进入随机生成的地图，在有限视野和资源条件下进行移动、战斗、拾取、探索与撤离。

项目支持单人游玩，并计划逐步加入多人联机。玩家人数不足时，可由不同版本的 AI 玩家补位。服务器会记录完整对局数据，用于战绩统计、对局回放、行为分析和 AI 模型训练。

这个项目的目标不只是制作一个小游戏，而是以游戏为载体，独立完成一套相对完整的软件系统，包括：

- 游戏客户端；
- 后端服务；
- 网络通信；
- 数据库设计；
- 用户与对局系统；
- AI 决策与深度学习；
- 数据分析看板；
- 自动化测试与部署；
- 技术文档和实验报告。

最终成果应当既能运行、能游玩、能部署，也能作为个人作品展示完整的工程能力和学习过程。

---

## 2. 为什么要做这个项目

本项目主要服务于以下目标：

### 2.1 建立完整的软件工程能力

不只学习单个框架，而是理解多个系统如何协同工作：客户端如何连接服务器，服务器如何保存数据，数据库如何支持业务，AI 模型如何进入真实应用，系统如何测试和部署。

### 2.2 通过真实问题学习技术

项目中的知识不会以孤立练习的形式出现，而会对应具体问题：

- 玩家如何连接房间；
- 多个客户端如何保持相同的游戏状态；
- 断线后如何恢复；
- 服务器如何验证玩家操作；
- 对局数据如何设计和查询；
- 如何让 AI 学习玩家行为；
- 如何评价不同 AI 的强弱；
- 如何把模型部署到后端推理。

### 2.3 形成可持续开发的长期项目

项目采用阶段式开发。每个阶段都应产生一个可以运行或演示的结果。即使最终没有实现所有高级功能，前面完成的部分也应当构成一个独立、完整、有展示价值的作品。

### 2.4 训练独立解决问题的能力

AI 可以辅助分析和生成代码，但所有核心模块都应由开发者理解、测试和维护。项目最终要体现的是开发者能够做出技术选择、定位错误、解释系统设计并持续迭代，而不是简单拼接 AI 生成的代码。

---

## 3. 核心玩法构想

## 3.1 基本形式

- 2D 俯视角；
- 网格地图；
- 回合制或固定 Tick 驱动；
- 单局时长目标为 10～15 分钟；
- 支持 1～4 名玩家；
- 支持 AI 队友或 AI 对手；
- 地图随机生成；
- 每局资源、敌人和路线不同；
- 玩家需要在生存、探索、战斗和撤离之间做决策。

选择网格和回合制，是为了将开发重点放在游戏逻辑、网络同步、数据系统和 AI 决策上，而不是过早陷入高帧率动作同步、复杂物理和美术制作。

## 3.2 单局流程

一次完整对局暂定为：

1. 玩家登录并进入大厅；
2. 创建或加入房间；
3. 选择角色或基础能力；
4. 服务器根据随机种子生成地图；
5. 玩家依次执行移动、攻击、拾取、使用技能或等待；
6. 敌人或 AI 玩家执行行动；
7. 玩家探索地图、获得资源并完成目标；
8. 玩家到达撤离点，或因全员死亡而失败；
9. 服务器结算对局；
10. 保存战绩、事件记录、回放数据和 AI 训练样本。

## 3.3 初始玩法范围

首个可玩版本只需要包含：

- 1 种地图主题；
- 1 个玩家角色；
- 3 种敌人；
- 2 种武器；
- 5～10 种道具；
- 基础生命值和伤害系统；
- 移动、攻击、拾取、使用、等待五类动作；
- 一个明确的胜利条件；
- 一个明确的失败条件。

首个版本不追求大量内容，而追求完整闭环。

---

## 4. 核心特色

## 4.1 服务端权威游戏逻辑

客户端只提交操作意图，例如“向上移动”或“攻击目标”。服务器负责判断操作是否合法、计算伤害、更新地图状态并广播结果。

这样可以减少客户端作弊，也能让多人对局、回放和 AI 训练使用同一套规则。

## 4.2 可回放的事件系统

每局游戏将被记录为按顺序排列的事件，例如：

```json
{
  "turn": 12,
  "actor_id": "player_1",
  "action": "attack",
  "target_id": "enemy_3",
  "result": {
    "damage": 8,
    "remaining_hp": 14
  }
}
```

通过保存随机种子、初始状态和动作事件，可以重建整局比赛，而不需要持续录制视频。

事件系统同时服务于：

- 对局回放；
- Bug 定位；
- 战绩统计；
- 玩家行为分析；
- AI 训练数据生成；
- 不同版本游戏逻辑的对比测试。

## 4.3 多层次 AI 玩家

项目不会只接入一个对话模型，而会逐步实现真正参与游戏决策的 AI：

1. 随机 AI；
2. 规则 AI；
3. 搜索或评分函数 AI；
4. 行为克隆模型；
5. 强化学习模型；
6. 不同模型之间的自动对战和评估。

AI 的目标不是简单地“表现得像人”，而是形成可以训练、测试、比较和部署的决策系统。

## 4.4 数据驱动的平衡与分析

后端将保存对局结果、角色选择、伤害、移动、资源获取、死亡原因、AI 版本和模型推理数据。

这些数据可以用于回答：

- 哪种武器胜率过高；
- 哪种敌人造成最多失败；
- 玩家最常在哪个阶段死亡；
- 规则 AI 和神经网络 AI 的差距有多大；
- 模型推理耗时是否影响服务端性能；
- 地图生成是否存在不公平情况。

---

## 5. 技术架构

```text
Godot 游戏客户端
    │
    ├── HTTP API
    │   ├── 注册与登录
    │   ├── 玩家信息
    │   ├── 战绩查询
    │   └── 排行榜
    │
    └── WebSocket
        ├── 房间状态
        ├── 玩家操作
        ├── 游戏事件
        ├── 心跳与断线恢复
        └── 对局状态广播
                │
         FastAPI 服务端
        ┌───────┼────────┐
        │       │        │
    业务 API  游戏核心  AI 推理
        │       │        │
        └───────┼────────┘
                │
           PostgreSQL
                │
       React 数据分析看板
```

## 5.1 游戏客户端

技术：Godot 4、GDScript。

主要职责：

- 渲染地图、角色、敌人和动画；
- 收集玩家输入；
- 展示服务端确认后的游戏状态；
- 管理界面、音效和基础交互；
- 连接 HTTP API 和 WebSocket；
- 支持本地单机调试与在线模式。

客户端不应拥有决定胜负的最终权力。

## 5.2 后端服务

技术：Python、FastAPI、Pydantic、SQLAlchemy、Alembic。

主要职责：

- 用户注册、登录和鉴权；
- 房间创建、加入和退出；
- WebSocket 连接管理；
- 游戏状态和回合推进；
- 玩家操作校验；
- 敌人与 AI 决策；
- 战绩结算；
- 数据持久化；
- 对局回放接口；
- 模型版本和实验结果管理。

## 5.3 数据库

技术：PostgreSQL。

初步数据实体：

- `users`：账号信息；
- `player_profiles`：昵称、等级、设置；
- `rooms`：房间信息；
- `matches`：对局概况；
- `match_players`：对局参与者；
- `match_events`：对局事件；
- `map_seeds`：地图种子和生成参数；
- `model_versions`：AI 模型版本；
- `model_metrics`：模型评估指标；
- `leaderboard_entries`：排行榜数据。

第一版只实现支撑当前功能所需的数据表，不提前设计复杂的通用平台。

## 5.4 AI 与深度学习

技术：PyTorch，后期可导出 ONNX。

AI 模块分为：

- 游戏环境封装；
- 状态特征提取；
- 规则策略；
- 数据集生成；
- 模型训练；
- 离线评估；
- 在线推理；
- 模型版本管理；
- 自动对战。

## 5.5 数据看板

技术：React、TypeScript。

主要页面：

- 总对局数和活跃玩家；
- 胜率和平均局长；
- 武器、角色和敌人统计；
- 单局事件时间线；
- 对局回放入口；
- AI 模型版本对比；
- 训练曲线和评估结果；
- 服务端性能指标。

## 5.6 部署与工程化

技术：Docker Compose、GitHub Actions、Linux。

目标：

- 本地一条命令启动后端和数据库；
- 自动运行格式检查和测试；
- 配置通过环境变量管理；
- 密钥不进入 Git 仓库；
- `main` 分支始终保持可运行；
- 每个阶段能够生成可演示版本。

---

## 6. AI 系统详细构想

## 6.1 状态空间

AI 可以读取的状态包括：

- 自身位置、生命值、状态和技能冷却；
- 可见区域中的地图格子；
- 敌人位置、类型和生命值；
- 队友位置和状态；
- 可拾取资源；
- 撤离点位置；
- 当前回合；
- 历史若干步动作。

第一版优先使用结构化特征，而不是直接输入游戏截图。

## 6.2 动作空间

初始动作空间：

- 向上移动；
- 向下移动；
- 向左移动；
- 向右移动；
- 普通攻击；
- 拾取；
- 使用道具；
- 使用技能；
- 等待。

无效动作应由服务端拒绝，或通过动作掩码在模型决策前排除。

## 6.3 规则 AI

规则 AI 作为最早的可用 AI 和后续模型的基线。

可能的规则：

- 生命值过低时优先治疗或撤退；
- 可击杀敌人时优先攻击；
- 与高伤害敌人保持距离；
- 没有威胁时靠近目标或资源；
- 使用 A* 搜索路径；
- 根据风险、收益和距离计算动作分数。

## 6.4 行为克隆

记录规则 AI 或真人玩家的状态与动作，构建监督学习数据集：

```text
游戏状态 -> 玩家选择的动作
```

训练目标是预测动作分类。需要处理：

- 数据清洗；
- 特征归一化；
- 动作类别不平衡；
- 训练集和验证集划分；
- 模型过拟合；
- 离线准确率与实际胜率不一致；
- 推理速度和批处理。

## 6.5 强化学习

在游戏规则稳定后，将游戏核心封装成可重复运行的训练环境。

可能的奖励：

- 造成伤害；
- 击败敌人；
- 保持生存；
- 获得关键资源；
- 到达撤离点；
- 完成团队目标；
- 减少无意义移动；
- 避免非法动作。

强化学习属于后期增强目标，不应阻塞基础游戏完成。

## 6.6 AI 评估指标

至少记录：

- 胜率；
- 平均生存回合；
- 平均造成伤害；
- 目标完成率；
- 非法动作比例；
- 平均每步推理耗时；
- 不同地图种子下的稳定性；
- 对不同 AI 对手的表现。

---

## 7. 代码仓库结构

```text
neurogrid/
├── client/                    # Godot 客户端
├── server/                    # FastAPI 后端和游戏服务器
├── ai/                        # 训练环境、模型和评估代码
├── dashboard/                 # React 数据看板
├── infra/                     # Docker、反向代理和部署配置
├── docs/                      # 架构、协议、数据库和决策记录
├── scripts/                   # 初始化、测试和数据处理脚本
├── tests/                     # 跨模块测试
├── .github/                   # Workflow、Issue 和 PR 模板
├── .env.example               # 环境变量示例
├── docker-compose.yml
├── ROADMAP.md
├── CONTRIBUTING.md
└── README.md
```

项目采用 Monorepo，便于一个人统一管理客户端、服务端、AI 和文档。

---

## 8. 开发阶段与完成标准

## M0：项目地基

目标：建立可持续开发的仓库和环境。

完成标准：

- 建立目录结构；
- 创建 Godot 和 FastAPI 空项目；
- 配置 `.gitignore` 和 `.env.example`；
- 配置 Python 格式检查和基础测试；
- Docker Compose 能启动 PostgreSQL；
- README 和本说明文档存在；
- GitHub Issue、Milestone 和分支规范建立。

## M1：单机原型

目标：完成最小可玩的本地游戏。

完成标准：

- 玩家可以在网格中移动；
- 有墙体和碰撞；
- 有一种敌人；
- 可以攻击和受到伤害；
- 可以胜利或失败；
- 可以重新开始一局。

## M2：完整单局循环

目标：让游戏具备基础可玩性。

完成标准：

- 随机地图；
- 多种敌人；
- 道具和资源；
- A* 寻路；
- 明确的探索和撤离目标；
- 基础 UI 和结算界面；
- 固定随机种子可以复现对局。

## M3：在线联机

目标：实现服务端权威的多人游戏。

完成标准：

- 创建和加入房间；
- WebSocket 长连接；
- 服务端验证操作；
- 多客户端状态一致；
- 心跳和断线处理；
- AI 可补位；
- 基础网络测试。

## M4：数据系统

目标：保存、查询和回放对局。

完成标准：

- 用户系统；
- 对局和参与者数据；
- 事件日志；
- 战绩查询；
- 排行榜；
- 对局回放；
- 基础数据库索引和迁移。

## M5：可训练 AI

目标：形成数据到模型再到游戏的闭环。

完成标准：

- 随机 AI；
- 规则 AI；
- 自动生成训练数据；
- 行为克隆训练脚本；
- 模型评估脚本；
- 服务端加载模型；
- 模型版本可追踪。

## M6：强化学习实验

目标：验证强化学习在该环境中的效果。

完成标准：

- 标准化训练环境；
- 设计奖励函数；
- 完成至少一种算法实验；
- 自动对战；
- 训练曲线和结果对比；
- 分析失败案例。

## M7：作品发布

目标：形成可公开展示的完整项目。

完成标准：

- Docker 一键部署；
- 数据分析看板；
- 自动化测试；
- 架构图和数据库图；
- API 和网络协议文档；
- 演示视频；
- 技术总结；
- 可下载或可访问的游戏版本。

---

## 9. 最小可行产品

MVP 只要求完成以下闭环：

1. 玩家启动客户端；
2. 进入一个随机网格地图；
3. 可以移动、攻击和拾取；
4. 敌人能够寻路和攻击；
5. 玩家可以成功撤离或死亡；
6. 对局结果被保存；
7. 可以查看基础战绩；
8. AI 能够代替玩家完成基本操作。

MVP 不要求多人联机、强化学习、精美美术或复杂剧情同时完成。

---

## 10. 暂不实现的内容

为了控制范围，首个完整版本不计划实现：

- 3D 开放世界；
- MMORPG；
- 高速实时 PVP；
- 复杂物理系统；
- 语音聊天；
- 自研游戏引擎；
- 大量剧情和关卡；
- 数十个角色和数百件装备；
- Kubernetes 和复杂微服务；
- 区块链或虚拟资产；
- 仅作为噱头的大模型 NPC 对话。

新的功能必须证明它能增强核心玩法、技术学习或作品展示，否则先放入候选列表，而不是立即开发。

---

## 11. 开发原则

### 11.1 每次只完成一个小闭环

优先选择半小时到三小时可以完成的任务。较大的任务必须拆成多个 Issue。

### 11.2 `main` 始终可运行

所有功能通过独立分支和 Pull Request 合并。合并前至少完成基础测试和人工运行验证。

### 11.3 先正确，再优化

在功能和接口尚未稳定前，不进行过早性能优化和复杂抽象。

### 11.4 先规则系统，再机器学习

AI 模型依赖稳定的游戏规则、可重复环境和高质量数据。不能在游戏核心尚未完成时优先训练模型。

### 11.5 所有关键决策留下记录

重要技术选择写入 `docs/decisions/`，包括问题、候选方案、最终选择和原因。

### 11.6 AI 生成代码必须经过理解和测试

任何无法解释的代码都不应直接合并。AI 可以辅助，但不能替代对系统的理解。

---

## 12. 项目完成后的展示内容

最终项目应至少包含：

- 可运行的游戏客户端；
- 可部署的后端；
- 完整 GitHub 提交记录；
- 系统架构图；
- 数据库 ER 图；
- 网络消息协议；
- AI 训练与评估报告；
- 不同 AI 策略的对比结果；
- 对局回放演示；
- 性能和压力测试结果；
- Docker 部署说明；
- 三至五分钟演示视频；
- 项目复盘和后续规划。

适合使用的项目描述：

> NeuroGrid 是一个基于 Godot、FastAPI、PostgreSQL 和 PyTorch 构建的在线战术 Roguelike。项目采用服务端权威架构，支持随机地图、多人房间、事件回放、战绩分析和可训练 AI 玩家，并形成从对局数据采集到模型训练、评估和在线推理的完整闭环。

---

## 13. 下一次打开仓库时应该做什么

每次继续开发时按以下顺序检查：

1. 查看当前 Milestone；
2. 查看未完成的 `priority:high` Issue；
3. 优先选择一个 `size:S` 或 `size:M` 任务；
4. 阅读相关设计文档；
5. 创建独立功能分支；
6. 编写或修改代码；
7. 运行测试；
8. 更新文档；
9. 提交 Pull Request；
10. 在 Issue 中记录结果和下一步。

如果不知道下一步做什么，默认优先级为：

```text
可运行性 > 核心玩法 > 自动测试 > 联机 > 数据系统 > AI > 美术和扩展内容
```

---

## 14. 当前最先执行的任务

项目初始化后，第一批任务应为：

1. 初始化 Monorepo 目录；
2. 创建可运行的 Godot 空项目；
3. 创建 FastAPI `/health` 接口；
4. 使用 Docker Compose 启动 PostgreSQL；
5. 实现测试地图；
6. 实现玩家网格移动；
7. 编写第一版游戏状态数据结构；
8. 编写客户端与服务器消息协议草案；
9. 配置 Pytest 和 GitHub Actions；
10. 完成第一个可运行版本标签 `v0.1.0`。

---

## 15. 项目成功的定义

本项目不以功能数量作为唯一成功标准。

只要最终能够做到以下几点，就可以认为项目成功：

- 有一个真正可玩的完整版本；
- 能清楚解释系统架构；
- 能独立定位和修复主要问题；
- 有客户端、网络、后端和数据库实践；
- 至少完成一种真正参与决策的 AI；
- 有可复现的实验和数据；
- 有持续、清晰的 GitHub 开发记录；
- 能向他人展示并说明自己学到了什么。

项目可以在开发过程中调整，但所有调整都应继续服务于同一个核心目标：

> **独立完成一个具有完整工程链路、可玩性和 AI 技术深度的在线游戏项目，并在开发过程中系统学习现代软件开发。**
